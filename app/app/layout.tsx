import type { Metadata } from "next";
import { Inter, Geist_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "FleetTribe — Fleet Intelligence Platform",
  description:
    "FleetTribe transforms driver behaviour and vehicle telemetry into explainable risk and maintenance insights.",
  metadataBase: new URL("https://fleettribe.vercel.app"),
  openGraph: {
    title: "FleetTribe — Fleet Intelligence Platform",
    description: "Turn Fleet Telemetry Into Decisions.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
